# CI/CD Pipeline Environment Variables
# This file contains environment variables for CI/CD pipeline testing

# Database - Use in-memory SQLite for CI to avoid external dependencies
DATABASE_URL="file:./test-ci.db"
DIRECT_URL="file:./test-ci.db"

# Supabase - Mock values for CI
NEXT_PUBLIC_SUPABASE_URL="https://test-ci.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="test-ci-anon-key"
SUPABASE_SERVICE_ROLE_KEY="test-ci-service-role-key"

# NextAuth.js - Test configuration
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="test-ci-secret-key-for-testing-only"

# OAuth Providers - Mock values
GOOGLE_CLIENT_ID="test-google-client-id"
GOOGLE_CLIENT_SECRET="test-google-client-secret"
DISCORD_CLIENT_ID="test-discord-client-id"
DISCORD_CLIENT_SECRET="test-discord-client-secret"
GITHUB_CLIENT_ID="test-github-client-id"
GITHUB_CLIENT_SECRET="test-github-client-secret"

# Redis - Mock URL for CI
REDIS_URL="redis://localhost:6379"

# Analytics & Monitoring - Disabled for CI
SENTRY_DSN=""
GOOGLE_ANALYTICS_ID=""
GOOGLE_SITE_VERIFICATION=""

# App Configuration
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NODE_ENV="test"

# Rate Limiting - Relaxed for CI
RATE_LIMIT_MAX="1000"
RATE_LIMIT_WINDOW="900000"

# Data Sync - Mock values
GOMAFIA_API_URL="https://test-gomafia.pro/api"
SYNC_INTERVAL="300000"
GOMAFIA_BASE_URL="https://test-gomafia.pro"
SYNC_BATCH_SIZE="10"
SYNC_MAX_RETRIES="3"
SYNC_CRON_SCHEDULE="0 0 * * *"

# Email - Disabled for CI
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""

# File Upload - Test limits
MAX_FILE_SIZE="1048576"
ALLOWED_FILE_TYPES="image/jpeg,image/png"

# Security - Test keys
JWT_SECRET="test-jwt-secret-for-ci-only"
ENCRYPTION_KEY="test-encryption-key-for-ci-only"

# Test Configuration
TEST_TIMEOUT="30000"
TEST_DATABASE_URL="file:./test-ci.db"
